import { ColorItem, ColorPalette, Meta } from '@storybook/addon-docs';
import { PrimaryColor, Greyscale, SecondaryColor } from './Colors';
import { getReadableColor } from './getReadableColor';
import { ColorSampler, ColorSample } from './ColorsStoriesHelpers';
import * as Blue from '../@next/utilities/colors/blue';
import * as Green from '../@next/utilities/colors/green';
import * as Neutral from '../@next/utilities/colors/neutral';
import * as Orange from '../@next/utilities/colors/orange';
import * as Red from '../@next/utilities/colors/red';
import * as Yellow from '../@next/utilities/colors/yellow';

<Meta title="Utilities/Colors" />

# Color

```
import { PrimaryColor, Greyscale } from 'glints-aries';

const MyComponent = styled.div`
  background-color: ${PrimaryColor.glintsyellow};
  color: ${PrimaryColor.grey};
`;
```

## PrimaryColor

<ColorSampler>
  {Object.keys(PrimaryColor).map(colorName => (
    <ColorSample
      key={colorName}
      bgColor={PrimaryColor[colorName]}
      fontColor={getReadableColor(PrimaryColor[colorName])}
      name={colorName}
    />
  ))}
</ColorSampler>

## Greyscale

<ColorSampler>
  {Object.keys(Greyscale).map(colorName => (
    <ColorSample
      key={colorName}
      bgColor={Greyscale[colorName]}
      fontColor={getReadableColor(Greyscale[colorName])}
      name={colorName}
    />
  ))}
</ColorSampler>

## SecondaryColor

<ColorSampler>
  {Object.keys(SecondaryColor).map(colorName => (
    <ColorSample
      key={colorName}
      bgColor={SecondaryColor[colorName]}
      fontColor={getReadableColor(SecondaryColor[colorName])}
      name={colorName}
    />
  ))}
</ColorSampler>

# @next

## Brand Colors

<ColorSampler>
  <ColorSample key={Yellow.Brand} bgColor={Yellow.Brand} name="Yellow.Brand" />
  <ColorSample key={Red.Brand} bgColor={Red.Brand} name="Red.Brand" />
  <ColorSample key={Blue.Brand} bgColor={Blue.Brand} name="Blue.Brand" />
  <ColorSample key={Orange.Brand} bgColor={Orange.Brand} name="Orange.Brand" />
</ColorSampler>

## Product Colors

<ColorPalette>
  <ColorItem
    title="Blue"
    subtitle="Blue.CodeName"
    colors={Object.fromEntries(Object.entries(Blue).slice(1, 7).sort(
      (a, b) => parseInt(a[0].slice(1)) - parseInt(b[0].slice(1))
    ))}
  />
  <ColorItem
    title="Green"
    subtitle="Green.CodeName"
    colors={Object.fromEntries(Object.entries(Green).slice(0, 5).sort(
      (a, b) => parseInt(a[0].slice(1)) - parseInt(b[0].slice(1))
    ))}
  />
  <ColorItem
    title="Neutral Whites"
    subtitle="Neutral.CodeName"
    colors={Object.fromEntries(Object.entries(Neutral).sort(
      (a, b) => parseInt(a[0].slice(1)) - parseInt(b[0].slice(1))
    ).slice(5, 10))}
  />
  <ColorItem
    title="Neutral Blacks"
    subtitle="Neutral.CodeName"
    colors={Object.fromEntries(Object.entries(Neutral).sort(
      (a, b) => parseInt(a[0].slice(1)) - parseInt(b[0].slice(1))
    ).slice(0, 5))}
  />
  <ColorItem
    title="Orange"
    subtitle="Orange.CodeName"
    colors={Object.fromEntries(Object.entries(Orange).slice(1, 6).sort(
      (a, b) => parseInt(a[0].slice(1)) - parseInt(b[0].slice(1))
    ))}
  />
  <ColorItem
    title="Red"
    subtitle="Red.CodeName"
    colors={Object.fromEntries(Object.entries(Red).slice(0, 5).sort(
      (a, b) => parseInt(a[0].slice(1)) - parseInt(b[0].slice(1))
    ))}
  />
  <ColorItem
    title="Yellow"
    subtitle="Yellow.CodeName"
    colors={Object.fromEntries(Object.entries(Yellow).slice(1, 6))}
  />
</ColorPalette>
