import { Meta } from '@storybook/addon-docs';
import * as BorderRadius from './';
import * as Neutral from '../colors/neutral';

<Meta title="Utilities/@next/BorderRadius" />

# Border Radius

<table>
  <tr>
    <th></th>
    <th>Token name</th>
    <th>Value</th>
  </tr>
  {Object.entries(BorderRadius).map(([tokenName, value]) => (
    <tr>
      <td>
        <div
          style={{ background: Neutral.B85, height: '80px', width: '80px', borderRadius: value }}
        />
      </td>
      <td>{tokenName}</td>
      <td>{value}</td>
    </tr>
  ))}
</table>
