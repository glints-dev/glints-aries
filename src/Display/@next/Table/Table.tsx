import {
  IndexTable,
  IndexTableProps,
  useIndexResourceState,
} from '@shopify/polaris';

// const Table = ({ children, ...props }: IndexTableProps) => {
//   return <IndexTable {...props}>{children}</IndexTable>;
// };
export {
  IndexTable as Table,
  IndexTableProps as TableProps,
  useIndexResourceState,
};
