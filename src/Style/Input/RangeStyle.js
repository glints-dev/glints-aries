import styled from 'styled-components';

export const RangeContainer = styled.div`
  position: relative;
  height: 1em;

  input[type="range"] {
    position: absolute;
    left: 0;
    width: 100%;
  }
`;
