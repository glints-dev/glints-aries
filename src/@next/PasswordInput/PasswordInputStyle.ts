import styled from 'styled-components';

export const StyledSuffixContainer = styled.div`
  height: 20px;
  transform: translateX(4px);

  svg {
    cursor: pointer;
    height: 20px;
    width: 20px;
  }
`;
