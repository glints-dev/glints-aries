import { Meta } from '@storybook/addon-docs';
import { borderRadiuses } from './';
import * as Neutral from '../colors/neutral';

<Meta title="@next/Utilities/BorderRadius" />

# Border Radius

<table>
  <tr>
    <th></th>
    <th>Token name</th>
    <th>Value</th>
  </tr>
  {Object.entries(borderRadiuses).map(([tokenName, value]) => (
    <tr>
      <td>
        <div
          style={{
            background: Neutral.B85,
            height: '80px',
            width: '80px',
            borderRadius: value,
          }}
        />
      </td>
      <td>{tokenName}</td>
      <td>{value}</td>
    </tr>
  ))}
</table>
