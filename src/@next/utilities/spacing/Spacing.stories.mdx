import { Meta } from '@storybook/addon-docs';
import { spaces } from './';
import * as Neutral from '../colors/neutral';

<Meta title="@next/Utilities/Spacing" />

# Spacing

<table>
  <tr>
    <th></th>
    <th>Token name</th>
    <th>Value</th>
  </tr>
  {Object.entries(spaces).map(([tokenName, value]) => (
    <tr>
      <td style={{ display: 'flex' }}>
        ●
        <div
          style={{ background: Neutral.B85, height: '30px', width: value }}
        />●
      </td>
      <td>{tokenName}</td>
      <td>{value}</td>
    </tr>
  ))}
</table>
