import { Meta } from '@storybook/addon-docs';
import * as DropShadow from './';
import * as Neutral from '../colors/neutral';
import { borderRadius2 } from '../borderRadius';

<Meta title="@next/Utilities/DropShadow" />

# Drop Shadow

<table>
  <tr>
    <th></th>
    <th>Token name</th>
    <th>Value</th>
  </tr>
  {Object.entries(DropShadow).map(([tokenName, value]) => (
    <tr>
      <td>
        <div
          style={{
            background: 'white',
            height: '60px',
            width: '60px',
            borderRadius: borderRadius2,
            boxShadow: value,
          }}
        />
      </td>
      <td>{tokenName}</td>
      <td>{value}</td>
    </tr>
  ))}
</table>
