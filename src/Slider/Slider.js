/* @flow */
import React from 'react';
import { SliderContainer } from '../Style/SliderStyle';

const Slider = (props: Props) => {
  const { children } = props;

  return (
    <SliderContainer>
        asd
    </SliderContainer>
  );
}

export default Slider;
